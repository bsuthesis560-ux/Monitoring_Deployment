# BSU Personnel Monitoring — Control Panel (Launcher) Requirements
# Install with: pip install -r requirements_launcher.txt
#
# These are ONLY for the control_panel.py launcher GUI.
# The facial recognition service itself requires the packages in requirements.txt

customtkinter>=5.2.0
Pillow>=10.0.0
opencv-python>=4.8.0
python-dotenv>=1.0.0

# For building the .exe
pyinstaller>=6.0.0
