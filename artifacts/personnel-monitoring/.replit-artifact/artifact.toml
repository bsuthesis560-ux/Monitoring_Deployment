kind = "web"
previewPath = "/"
title = "Personnel Monitoring System"
version = "1.0.0"
id = "artifacts/personnel-monitoring"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19304

[services.development]
run = "pnpm --filter @workspace/personnel-monitoring run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/personnel-monitoring", "run", "build" ]
publicDir = "artifacts/personnel-monitoring/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19304"
BASE_PATH = "/"
